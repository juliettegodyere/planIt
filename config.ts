// config.ts

import { vars } from "nativewind";

export const config = {
    light: vars({
      '--color-primary-0': '51 51 51',
    }),
    dark: vars({
      '--color-primary-0': '240 240 240',
    })
  }